export type Config = {
  debounce: number;
  tsconfigPath: string | null;
  truncateLength: number;
    decoration: {
        color: string;
        opacity: string;
        fontStyle: string;
    }
}
